# SouthCoast Eats — Cleaner Home Screen

This version removes the large coastal hero image from the home screen for a simpler, faster mobile layout.

## Replace these files in GitHub

- `index.html`
- `styles.css`

Keep the existing:
- `brand-logo.jpg`
- `coastal-footer.jpg`

The `coastal-hero.jpg` file is no longer used by the home page, so it can remain in the repository or be removed later.

The existing buttons and selection behavior are preserved.
